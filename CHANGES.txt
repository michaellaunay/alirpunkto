Unreleased (2026-08-01)
-----------------------

- Profile visibility: non-admin members are restricted to their own
  profile — no member list, crafted oids neutralised, plain GET lands on
  one's page (#201); administrators get a fixed read-only card of eight
  fields and can never modify anyone (#149).
- One's own profile follows the issue #55 matrix: presentation, avatar,
  e-mail and languages editable by all; IBAN editable by Cooperators or
  assimilated, who also view — never edit — identity, nationality, CBM,
  shares, contribution validity and role; groups shown translated above
  the form; the erasure date visible to no one; a resigning member can
  still open their profile to cancel (#55).
- The profile view carries its specified frame: translated title,
  introduction, Submit and Cancel buttons (#123, reviving #116).
- The change-password screen displays in the member's preferred
  language for the link bearer, never for the anonymous leg (#248), and
  the forgot-password view is fully translated — the i18n domain now
  covers the whole template and the instruction msgid exists in the 33
  catalogues (#175, closing duplicate #86).
- The e-mail confirmation page carries its own title and interpolated
  variables instead of literal ${...} (#160).
- Repairs a production-only routing defect: a helper had slid between
  @view_config and the modify_member definition, structurally locked
  against recurrence.
- Long-term LDAP provisions: the Shared Directory fields of statutes
  5.3.1 — five destination-address slots and a ciphered personal-data
  block proven to fit its 512-character bound (440 for a maximal member,
  thanks to two-letter group/role codes) — and the Identity Recovery
  Code field, whose 64 characters are the SHA-256 of the code's
  canonical form, the secret itself never stored; all invisible by
  construction and deployable through tools/ldap_provision.py
  (#110, #127).
- The Cooperator minimum age is enforced at both doors with the
  ticket's own refusal message; the age bound is now recomputed at
  every request instead of freezing at process start (#80).
- The e-mail sender is never a person: an honest cascade (environment,
  then a respected .ini value, then welcome@<domain>) replaces a
  resolution that always overwrote the .ini down to a literal
  'default_sender' From (#69).
- pytest live logging becomes opt-in: green runs are silent, failing
  tests keep their captured logs.
- External audit (2026-08-01), counter-reviewed on code and filed
  bilingually under docs/*/audits/: the post-login redirect only
  follows same-site targets; login attempts are rate-limited before
  any LDAP work (with Waitress now trusting the Apache proxy);
  dependencies are bounded in pyproject.toml, pinned in
  requirements.lock and installed as such by the CI — setup.py is
  retired. Three decisions recorded: the ciphered DEBUG password logs
  stay, the constants_and_globals module is a deliberate choice, and
  Keycloak will not become the single authentication entry point.
- Login reads POST only — credentials in a crafted GET query string
  no longer reach authentication; .env is loaded exactly once at
  startup and the process environment takes precedence (get_key
  retired); the bootstrap LDIF is born 0600 with passwords readable
  from dedicated environment variables; every LDAP group write is
  checked and logged with member, group, operation and side; Keycloak
  calls carry timeouts and never log raw response bodies; the
  Waitress proxy options moved to [server:main], where they act.
- Quality gates in CI: ruff (Pyflakes) and bandit (medium+) block,
  pip-audit runs on the lock with one documented exception, a
  coverage floor at 68 (measured ~70), every GitHub action pinned by
  commit SHA, no out-of-lock installs left, mypy informative (124
  errors at adoption). The sweep fixed two real defects: a missing
  global broke a tool's fallback, and a stray requests import
  shadowed the view parameter.
- Fourth external-audit pass (7.1/10) filed bilingually with its
  counter-review and execution plan: the documented Docker stack
  still cannot start (setup.py checks in both start scripts, an
  unknown Waitress option verified to raise ValueError, a loopback
  listen, a wrong trusted_proxy for compose); a P0 patch with a
  Compose smoke test is the next train, then split hashed locks and
  a multi-stage image.

Unreleased (2026-07-30)
-----------------------

- Ordinary Members can upgrade to Cooperator: identity-only form, existing
  registration flow (verifiers, vote), in-place LDAP update (#7).
- Member resignation per the historical specification: double confirmation
  by e-mailed link, LDAP entry deactivated but kept through the Quarantine
  (180 days, §3.4), lazy expiry, cancellation, login guard, deferred purge
  retaining only pseudonym, date and reason, with an erasure notice (#54).
- Dynamic-group transitions: one pure truth table over shares,
  yearly-contribution validity, sanction and Board/MAC role; idempotent
  best-effort synchroniser wired to registration, upgrade, resignation and
  profile updates; daily scan for calendar-driven moves; legacy
  ordinaryMembersGroup drained progressively (#148, superseding #58, #65,
  #74, #76, #146).
- Identity uniqueness enforced for every Applicant against all LDAP
  entries, resigned Cooperators in Quarantine included (#54).
- Member avatar stored in LDAP jpegPhoto with dedicated upload/serve views
  and strict validation (#150).
- Home-page application catalogue filtered by member status, with direct
  SSO links (#142, #35); browser-language default (#171); profile
  description as textarea (#165); mandatory-field asterisks (#107).
- All campaign strings renamed to symbolic msgids across the 33 catalogues;
  alirpunkto.pot brought back in line.
- Test suite grown from ~580 to 800+.

Unreleased (2026-07-25)
-----------------------

- Site-wide i18n interpolation, recipient-language e-mails, verifier-language
  subjects, seven-language result e-mails and a new pending e-mail
  (#223, #236, #238, #204, #213, #214, PR #235).
- Tally rule: a tie of the verifier votes approves the application (PR #232).
- Password-reset page limited to the password fields (#97); SSO application
  links opening in a new tab with inline explanations (#142, #147, #194).
- Postal address and administrator e-mail interpolated in outgoing e-mails
  (#169, #81); verifier greeting fixed in forty templates (#217).
- GitHub Actions CI repaired (pip cache keyed on setup.py, 3.11/3.12 matrix);
  test suite grown from ~420 to 580+.

0.0
---

- Initial version.
